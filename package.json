{
  "scripts": {
    "dev": "npx parcel src/index.html --open --port 3000",
    "build": "npx parcel build src/index.html"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "parcel": "^2.4.1",
    "postcss": "^8.4.12",
    "tailwindcss": "^3.0.24"
  }
}
